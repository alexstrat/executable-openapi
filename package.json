{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna exec -- npm run test",
    "lint": "lerna exec -- npm run lint"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.3.2"
  },
  "dependencies": {}
}
